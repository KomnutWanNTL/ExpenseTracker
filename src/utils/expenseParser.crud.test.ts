import { describe, test } from 'vitest';

describe('Expense CRUD flow', () => {
	test.todo('shows newly added expenses first in the list');
	test.todo('removes an expense from the UI and localStorage when deleted');
	test.todo('updates note, amount, and category when editing an expense');
	test.todo('restores persisted expenses after a page reload');
});
